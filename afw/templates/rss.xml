<?xml version="1.0" ?>
<rss version="2.0">
    <channel>
        <title>{{ shop }}</title>
        <link>{{ link }}</link>
        <description>Anime Figures - {{ shop }}</description>
        <language>de</language>
        <copyright>{{ server_url }}</copyright>
        <lastBuildDate>{{ build_date }}</lastBuildDate>
        <generator>{{ server_url }}</generator>
        <image>
            <url>{{ favicon_path }}</url>
            <title>{{ shop }}</title>
            <link>{{ link }}</link>
        </image>
        {% for item in items %}
            <item>
                <title>{{ item['title'] }} {{ item['price'] | string }}</title>
                <link>
                    {{ item['url'] }}
                </link>
                <pubDate>{{ item['save_date'] }}</pubDate>
                <description>
                    &lt;img src=&#039;{{ item['image_path'] }}&#039; /&gt;
                    &lt;h2&gt;{{ item['title'] }}&lt;/h2&gt;
                    &lt;br&gt;{{ item['description'] }}&lt;/br&gt;
                </description>
                <guid>
                    {{ item['url'] }}
                </guid>
            </item>
        {% endfor %}
    </channel>
</rss>